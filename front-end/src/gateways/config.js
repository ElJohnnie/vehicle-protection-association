import banks from "./mocks/banks.json";

const config = {
  // endpoint: "https://api.brazilautoseg.com",
  endpoint: "http://localhost:3000",
  cep: "https://viacep.com.br/ws",
  fipe: "https://parallelum.com.br/fipe/api/v2",
  banks,
};

export default config;
